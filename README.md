# CheeseBot Status

Static status page for CheeseBot, published via GitHub Pages.

## Local refresh

```bash
python3 status_snapshot.py
python3 -m http.server 8080
# open http://localhost:8080
```

## Publish

This repo is intended for GitHub Pages (branch: `main`, folder: `/root`).
